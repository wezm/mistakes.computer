

Build
-----

Requires [Janet](https://janet-lang.org/) and [Jpm](https://github.com/janet-lang/jpm.git).
Install the `jpm` package on Chimera/Casuarina Linux.

    jpm deps
    jpm test
    jpm build

Executable is `build/mistakes`. Can also run without compiling: `janet server.janet`.
